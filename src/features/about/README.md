# About Page

This folder contains the editable About page content and its UI component.

Edit `about.config.tsx` to change:

- bio/details paragraph
- links inside the bio
- research section title
- research interest tags

The UI layout lives in `components/AboutContent.tsx`.
Change the config when you only want to update content.
